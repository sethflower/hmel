const cfg=window.WMS_CONFIG;export class ApiError extends Error{constructor(message,code){super(message);this.code=code}}
export const session={get token(){return localStorage.getItem('wms_token')||''},set token(v){v?localStorage.setItem('wms_token',v):localStorage.removeItem('wms_token')}};
export async function api(group,action,payload={}){if(!navigator.onLine)throw new ApiError('Нет соединения. Повторите сохранение после восстановления сети.','NETWORK');if(!cfg.API_URL||cfg.API_URL.includes('PASTE_'))throw new ApiError('Адрес API не настроен. Откройте frontend/config.js.','CONFIG');let response;try{response=await fetch(cfg.API_URL,{method:'POST',headers:{'Content-Type':'application/json;charset=utf-8'},body:JSON.stringify({group,action,token:session.token,payload})});}catch(e){throw new ApiError('Нет соединения. Повторите сохранение после восстановления сети.','NETWORK')}let json;try{json=await response.json()}catch(e){throw new ApiError('Сервер вернул некорректный ответ','RESPONSE')}if(!json.success){if(json.error?.code==='AUTH')session.token='';throw new ApiError(json.error?.message||'Не удалось выполнить операцию',json.error?.code)}return json.data}
