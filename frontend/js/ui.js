export const $=(s,r=document)=>r.querySelector(s);export const $$=(s,r=document)=>[...r.querySelectorAll(s)];export const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));export const date=v=>v?new Intl.DateTimeFormat('ru-RU',{dateStyle:'short',timeStyle:String(v).includes('T')?'short':undefined}).format(new Date(v)): '—';export const badge=s=>`<span class="badge" data-status="${esc(s)}">${esc(s)}</span>`;
export function toast(message,type='ok'){const x=document.createElement('div');x.className='toast '+type;x.textContent=message;$('#toasts').append(x);setTimeout(()=>x.remove(),3500)}
export function modal(html,onOpen){$('#modal-content').innerHTML=html;$('#modal').classList.remove('hidden');onOpen?.($('#modal-content'))}export function closeModal(){$('#modal').classList.add('hidden');$('#modal-content').innerHTML=''}
export function formData(form){return Object.fromEntries(new FormData(form).entries())}export function table(headers,rows){return `<div class="table-wrap"><table><thead><tr>${headers.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr>${r.map((c,i)=>`<td data-label="${esc(headers[i])}">${c}</td>`).join('')}</tr>`).join(''):`<tr><td colspan="${headers.length}" class="empty">Нет данных</td></tr>`}</tbody></table></div>`}
export function confirmBox(text){return new Promise(resolve=>modal(`<h2>Подтверждение</h2><p>${esc(text)}</p><div class="actions"><button class="danger" data-yes>Подтвердить</button><button class="secondary" data-no>Отмена</button></div>`,root=>{root.querySelector('[data-yes]').onclick=()=>{closeModal();resolve(true)};root.querySelector('[data-no]').onclick=()=>{closeModal();resolve(false)}}))}
